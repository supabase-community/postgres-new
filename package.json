{
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace postgres-new"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "supabase": "^1.187.8"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.1",
    "@vercel/kv": "^2.0.0"
  }
}
